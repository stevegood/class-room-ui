let Page = require('./Page.jsx');

let Home = React.createClass({
  render: function() {
    return (
      <Page className="home" title="Class Room Organizer">
      </Page>
    );
  }
});

module.exports = Home;
